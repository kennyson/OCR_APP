# Your app's public URL (no trailing slash)
BASE_URL=https://ocr-app-axpq.onrender.com

# Box OAuth credentials (from Developer Console → Configuration → OAuth 2.0 Credentials)
BOX_CLIENT_ID=
BOX_CLIENT_SECRET=

# Optional: fixed session secret (auto-generated if not set)
SESSION_SECRET=
